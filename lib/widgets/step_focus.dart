import 'package:flutter/material.dart';

class StepFocus extends StatelessWidget {
  const StepFocus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
